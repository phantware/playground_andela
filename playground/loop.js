// Loop and add stuff to a process without  changing it contents
// process.env.NODE_ENV = res;
// add all property names and values to process.env

// for (const prop in res) {
//   process.env[prop] = res[prop];
//   console.log(prop);
// }
// const { NODE_ENV, PORT, CONNECTION_STRING } = process.env;
// console.log(NODE_ENV, PORT, CONNECTION_STRING);
